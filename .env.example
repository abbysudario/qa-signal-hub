# n8n webhook URL — the endpoint that receives CI signals from Lighthouse
# get this from your n8n workflow's webhook node
N8N_WEBHOOK_URL=

# GitHub personal access token — used by n8n to create and search GitHub Issues
# generate at https://github.com/settings/tokens
# required scopes: repo
GITHUB_TOKEN=